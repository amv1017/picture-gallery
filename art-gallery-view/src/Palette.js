const Palette = ({ paint, onClick }) => {
    return(
    <div className="" onClick={onClick}>
       
    
            <img src={paint.url} />
            
       
    </div>
    )
}

export default Palette
